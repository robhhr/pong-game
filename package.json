{
  "name": "pong-svg",
  "version": "0.1.0",
  "description": "An SVG Pong game.",
  "license": "ISC",
  "author": "Roberto H robhhr@outlook.com",
  "main": "src/index.js",
  "scripts": {
    "start": "parcel index.html -p 3000",
    "predeploy": "rm -rf dist && parcel build index.html --public-url /pong-game/",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "parcel-bundler": "^1.12.3"
  }
}
